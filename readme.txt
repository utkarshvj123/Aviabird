following are the steps to solve problems:

1) Use node file name to run the project.
2) using fs library for reading the file.
3) Then, divide the file in three parts by:
        a) '---'
         b) "READMORE"
    
4) Then, convert the selected data in JSON
